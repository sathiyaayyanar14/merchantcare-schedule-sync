export { getDatabase, createDatabaseAdapter } from './factory';
export type { DatabaseAdapter, DatabaseQuery, User, Session, AuthResponse, DatabaseProvider } from './types';
export { databaseConfig } from './config';